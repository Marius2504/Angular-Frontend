import { ElementRef } from '@angular/core';
import { SettDirective } from './sett.directive';

describe('SettDirective', () => {
  it('should create an instance', () => {
    const directive = new SettDirective();
    expect(directive).toBeTruthy();
  });
});
